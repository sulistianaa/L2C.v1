@model IEnumerable<InventoryGudang.Models.Reason>

@{
    ViewData["Title"] = "Reasons";
}

<h1>Reasons</h1>

@if (User.IsInRole("Admin"))
{
    <p>
        <a asp-action="Create" class="btn btn-primary">Create New</a>
    </p>
}

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            @if (User.IsInRole("Admin"))
            {
                <th></th>
            }
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Description)
                </td>
                @if (User.IsInRole("Admin"))
                {
                    <td>
                        <a href="@Url.Action("Edit", new { id = item.ReasonId })">Edit</a> |
                        <a href="@Url.Action("Delete", new { id = item.ReasonId })">Delete</a>
                    </td>
                }
            </tr>
        }
    </tbody>
</table>